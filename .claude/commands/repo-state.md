Spawn the `brain-campaign-state` agent and report its verdict plus punch list
verbatim.

Read-only diagnostic. Do not run mission steps based on this output without an
explicit user instruction. If the verdict is `STOP` or `USER-JUDGMENT`, advise
the user against running `go` until the drift is resolved.
